<h1>Cadastro de Apresentação</h1>
<form action="/apresentacao" method="POST">
    <div class="mb-3">
        <label for="nome" class="form-label">Nome:</label>
        <input type="text" id="nome" name="nome" class="form-control" required>
    </div>
        
    <div class="mb-3">
        <label for="usuario" class="form-label">Participantes:</label>
        <select name="idUsuario" id="selectParticipante" class="form-select" style="pointer-events: none;">
            <option value="{{usuario.dataValues.idUsuario}}">{{usuario.dataValues.nome}} - {{usuario.dataValues.idUsuario}}</option>
        </select>
    </div>

    <div class="mb-3">
        <label for="selectParticipante" class="form-label">Selecione os participantes:</label>
        <select name="idUsuario" id="selectParticipante" class="form-select">
            <option value="">Selecionar</option>
            {{#each nome}}
            <option value="{{idUsuario}}">{{nome}} - {{idUsuario}}</option>
            {{/each}}
        </select>
    </div>

    <div class="mb-3">
        <label for="selectParticipante" class="form-label">Selecione os participantes:</label>
        <select name="idUsuario" id="selectParticipante" class="form-select">
            <option value="">Selecionar</option>
            {{#each nome}}
            <option value="{{idUsuario}}">{{nome}} - {{idUsuario}}</option>
            {{/each}}
        </select>
    </div>

    <div class="mb-3">
        <label for="selectParticipante" class="form-label">Selecione os participantes:</label>
        <select name="idUsuario" id="selectParticipante" class="form-select">
            <option value="">Selecionar</option>
            {{#each nome}}
            <option value="{{idUsuario}}">{{nome}} - {{idUsuario}}</option>
            {{/each}}
        </select>
    </div>

    <div class="mb-3">
        <label for="selectParticipante" class="form-label">Selecione os participantes:</label>
        <select name="idUsuario" id="selectParticipante" class="form-select">
            <option value="">Selecionar</option>
            {{#each nome}}
            <option value="{{idUsuario}}">{{nome}} - {{idUsuario}}</option>
            {{/each}}
        </select>
    </div>

    <div class="mb-3">
        <label for="selectParticipante" class="form-label">Selecione os participantes:</label>
        <select name="idUsuario" id="selectParticipante" class="form-select">
            <option value="">Selecionar</option>
            {{#each nome}}
                <option value="{{idUsuario}}">{{nome}} - {{idUsuario}}</option>
            {{/each}}
        </select>
    </div>

    <button type="submit" class="btn btn-success">Cadastrar</button>
    <a href="/apresentacao"><button type="button" class="my-4 btn btn-danger">Voltar</button></a>
</form>