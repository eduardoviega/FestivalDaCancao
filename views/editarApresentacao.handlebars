<h1>Editar Apresentação:</h1>
<form action="/editar/{{idApresentacao}}" method="POST">
    <div class="mb-3">
        <label for="nome" class="form-label">Nome:</label>
        <input type="text" id="nome" name="nome" class="form-control" required>
    </div>
     
    <div class="mb-3">
        <label for="selectParticipante" class="form-label">Selecione os participantes:</label>
        <select name="idUsuario" id="selectParticipante" class="form-select">
            <option value="">Selecionar</option>
            {{#each nome}}
            <option value="{{idUsuario}}">{{nome}} - {{idUsuario}}</option>
            {{/each}}
        </select>
    </div>
    
    <div class="mb-3">
        <label for="selectParticipante" class="form-label">Selecione os participantes:</label>
        <select name="idUsuario" id="selectParticipante" class="form-select">
            <option value="">Selecionar</option>
            {{#each nome}}
            <option value="{{idUsuario}}">{{nome}} - {{idUsuario}}</option>
            {{/each}}
        </select>
    </div>

    <div class="mb-3">
        <label for="selectParticipante" class="form-label">Selecione os participantes:</label>
        <select name="idUsuario" id="selectParticipante" class="form-select">
            <option value="">Selecionar</option>
            {{#each nome}}
            <option value="{{idUsuario}}">{{nome}} - {{idUsuario}}</option>
            {{/each}}
        </select>
    </div>

    <div class="mb-3">
        <label for="selectParticipante" class="form-label">Selecione os participantes:</label>
        <select name="idUsuario" id="selectParticipante" class="form-select">
            <option value="">Selecionar</option>
            {{#each nome}}
            <option value="{{idUsuario}}">{{nome}} - {{idUsuario}}</option>
            {{/each}}
        </select>
    </div>

    <div class="mb-3">
        <label for="selectParticipante" class="form-label">Selecione os participantes:</label>
        <select name="idUsuario" id="selectParticipante" class="form-select">
            <option value="">Selecionar</option>
            {{#each nome}}
            <option value="{{idUsuario}}">{{nome}} - {{idUsuario}}</option>
            {{/each}}
        </select>
    </div>

    <div class="mb-3">
        <label for="selectParticipante" class="form-label">Selecione os participantes:</label>
        <select name="idUsuario" id="selectParticipante" class="form-select">
            <option value="">Selecionar</option>
            {{#each nome}}
            <option value="{{idUsuario}}">{{nome}} - {{idUsuario}}</option>
            {{/each}}
        </select>
    </div>

    <button type="submit" class="btn btn-success">Salvar</button>
    <a href="/apresentacao"><button type="button" class="btn btn-danger">Voltar</button></a>
</form>