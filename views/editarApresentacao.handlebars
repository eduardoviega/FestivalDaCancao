<h1>Editar Apresentação {{idApresentacao}}</h1>
<form action="/editarApresentacao/{{idApresentacao}}" method="POST">
    <div class="mb-3">
        <label for="nome" class="form-label">Nome:</label>
        <input type="text" id="nome" name="nome" class="form-control" required>
    </div>
        
    <div class="mb-3">
        <label for="usuario" class="form-label">Participantes:</label>
        <input type="text" id="usuario" name="idUsuario" value="{{usuarioLogado}}" class="form-control" readonly>
    </div>

    <div class="mb-3">
        <label for="selectParticipante" class="form-label">Selecione os participantes:</label>
        <select name="idUsuario" id="selectParticipante" class="form-select">
            <option value="">Selecionar</option>
            {{#each nome}}
            <option value="{{idUsuario}}">{{nome}} - {{idUsuario}}</option>
            {{/each}}
        </select>
    </div>

    <div class="mb-3">
        <label for="selectParticipante" class="form-label">Selecione os participantes:</label>
        <select name="idUsuario" id="selectParticipante" class="form-select">
            <option value="">Selecionar</option>
            {{#each nome}}
            <option value="{{idUsuario}}">{{nome}} - {{idUsuario}}</option>
            {{/each}}
        </select>
    </div>

    <div class="mb-3">
        <label for="selectParticipante" class="form-label">Selecione os participantes:</label>
        <select name="idUsuario" id="selectParticipante" class="form-select">
            <option value="">Selecionar</option>
            {{#each nome}}
            <option value="{{idUsuario}}">{{nome}} - {{idUsuario}}</option>
            {{/each}}
        </select>
    </div>

    <div class="mb-3">
        <label for="selectParticipante" class="form-label">Selecione os participantes:</label>
        <select name="idUsuario" id="selectParticipante" class="form-select">
            <option value="">Selecionar</option>
            {{#each nome}}
            <option value="{{idUsuario}}">{{nome}} - {{idUsuario}}</option>
            {{/each}}
        </select>
    </div>

    <div class="mb-3">
        <label for="selectParticipante" class="form-label">Selecione os participantes:</label>
        <select name="idUsuario" id="selectParticipante" class="form-select">
            <option value="">Selecionar</option>
            {{#each nome}}
            <option value="{{idUsuario}}">{{nome}} - {{idUsuario}}</option>
            {{/each}}
        </select>
    </div>

    <button type="submit">Atualizar</button>